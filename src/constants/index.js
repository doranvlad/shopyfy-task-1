import { Controller, Navigation } from "swiper";

export const selector = {
  header: "header",
  announcementBar: ".announcement-bar",
  filtersAnchor: "[data-filters-anchor]",
  filtersFaq: "[data-filters-faq]",
  facetFiltersForm: "#FacetFiltersForm",
};

export const colorOptionNames = ["Color", "Farbe", "Couleur"];

window.colorOptionNames = colorOptionNames;
